import cn from 'classnames';

const inputContainerStyles = cn('mt-auto');
const formGroupStyles = cn('form-group');
const inputGroupStyles = cn('input-group');

const inputStyles = cn(
  'mr-2',
  'form-control',
);

const inputBtnStyles = cn(
  'btn',
  'btn-primary',
);

const inputFeedbackStyles = cn(
  'd-block',
  'invalid-feedback',
);

export {
  inputContainerStyles,
  formGroupStyles,
  inputGroupStyles,
  inputStyles,
  inputBtnStyles,
  inputFeedbackStyles,
};
